---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "laravel_cloud_background_process Resource - Laravel Cloud"
subcategory: ""
description: |-
  Manages a background process (worker or custom daemon) on a Laravel Cloud instance.
---

# laravel_cloud_background_process (Resource)

Manages a background process (worker or custom daemon) on a Laravel Cloud instance.

## Example Usage

```terraform
resource "laravel_cloud_application" "example" {
  name                         = "my-app"
  repository                   = "my-org/my-app"
  region                       = "us-east-2"
  source_control_provider_type = "github"
}

resource "laravel_cloud_environment" "example" {
  application_id = laravel_cloud_application.example.id
  name           = "production"
  branch         = "main"

  php_version  = "8.4:1"
  node_version = "22"

  uses_push_to_deploy = true
}

resource "laravel_cloud_instance" "example" {
  environment_id = laravel_cloud_environment.example.id
  name           = "web"
  size           = "flex.c-1vcpu-256mb"
  scaling_type   = "none"
  min_replicas   = 1
  max_replicas   = 1
}

resource "laravel_cloud_background_process" "example" {
  instance_id = laravel_cloud_instance.example.id
  type        = "worker"
  processes   = 2
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `instance_id` (String) Parent instance ID.
- `processes` (Number) Number of processes (1-10).
- `type` (String) Process type (worker, custom).

### Optional

- `command` (String) Custom command (required when type is custom, 3-500 chars).
- `config` (String) JSON-encoded worker configuration (connection, queue, tries, backoff, sleep, rest, timeout, force).

### Read-Only

- `created_at` (String)
- `id` (String) The ID of this resource.
- `strategy_threshold` (Number) Scaling strategy threshold (null when no autoscaling strategy is set).
- `strategy_type` (String) Scaling strategy type (none, growth_rate, queue_size).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import laravel_cloud_background_process.example "{background_process_id}"
```
