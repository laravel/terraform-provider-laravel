---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "laravel_cloud_environment_variables Resource - Laravel Cloud"
subcategory: ""
description: |-
  Manages environment variables for a Laravel Cloud environment. Keys in this map are written on every apply, and a key removed from the map is deleted from the environment. Variables set outside Terraform are left alone.
---

# laravel_cloud_environment_variables (Resource)

Manages environment variables for a Laravel Cloud environment. Keys in this map are written on every apply, and a key removed from the map is deleted from the environment. Variables set outside Terraform are left alone.

## Example Usage

```terraform
resource "laravel_cloud_application" "example" {
  name                         = "my-app"
  repository                   = "my-org/my-app"
  region                       = "us-east-2"
  source_control_provider_type = "github"
}

resource "laravel_cloud_environment" "example" {
  application_id = laravel_cloud_application.example.id
  name           = "production"
  branch         = "main"

  php_version  = "8.4:1"
  node_version = "22"

  uses_push_to_deploy = true
}

# One resource manages the full variable set for an environment. Variables not
# listed here are removed on apply.
resource "laravel_cloud_environment_variables" "example" {
  environment_id = laravel_cloud_environment.example.id

  variables = {
    APP_ENV   = "production"
    LOG_LEVEL = "info"
  }
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `environment_id` (String) Environment ID.
- `variables` (Map of String, Sensitive) Map of environment variable key-value pairs. Removing a key deletes that variable from the environment.

### Read-Only

- `id` (String) Computed identifier (same as environment_id).
